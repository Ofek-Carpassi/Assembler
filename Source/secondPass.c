/* Execution of the second pass of the assembler */

#include <stdio.h>

/**
 * @brief This function is the second pass of the assembler.
 *
 * @param inputFile The input file.
 * @param outputFile The output file.
 * @return an array of the numberical values of the labels.
 */

/* The second pass "Identify the labels that appear in the code and give each label a numerical value that is the address in memory that the symbol represents." */
int secondPass(FILE *inputFile, FILE *outputFile)
{
    /* Identify the labels that appear in the code and give each label a numerical value that is the address in memory that the symbol represents. */
    /* Write the file with the replaced labels to the output file. */
    /* Return an array of the numberical values of the labels. */
}