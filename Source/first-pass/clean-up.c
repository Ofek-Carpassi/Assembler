/* Execute the cleaning process. */

#include <stdio.h>

/**
 * @brief This function cleans all the white spaces and comments from the input file.
 *
 * @param inputFile The input file.
 * @param outputFile The output file.
 * @return 0 On successful execution.
*/
int cleanUp(FILE *inputFile, FILE *outputFile)
{
    /* Clean all the white spaces and comments from the input file. */
    /* Write the clean file to the output file. */
    /* Return 0 for successful execution. */
}