/* Execute the error checking. */

#include <stdio.h>

/**
 * @brief This function checks for errors in the input file.
 *
 * @param inputFile The input file.
 * @return 0 for no errors, else the right error code (will be kept in a header file).
 */
int checkForErrors(FILE *inputFile)
{
    /* Check for errors in the input file. */
    /* Return the right error code. */
}